// @compile target=exe
// Lemon 示例 - 字符串操作

package examples;

public class StringExample {
    public static void main(String[] args) {
        System.printf("=== 字符串操作示例 ===\n\n");

        // 1. 字符串赋值与输出
        System.printf("1. 字符串赋值与输出:\n");
        String greeting = "Hello, Lemon!";
        System.printf("   字符串: %s\n", greeting);
        System.printf("   长度: %d\n\n", greeting.length());

        // 2. 字符串拼接
        System.printf("2. 字符串拼接:\n");
        String first = "Hello";
        String second = "World";
        String combined = first.concat(" ").concat(second);
        System.printf("   \"%s\" + \" \" + \"%s\" = \"%s\"\n\n", first, second, combined);

        // 3. 子串提取
        System.printf("3. 子串提取:\n");
        String text = "Hello, World!";
        System.printf("   原始: %s\n", text);
        System.printf("   子串(0, 5): %s\n", text.substring(0, 5));
        System.printf("   子串(7, 12): %s\n\n", text.substring(7, 12));

        // 4. 字符查找
        System.printf("4. 字符查找:\n");
        System.printf("   indexOf(\"W\"): %d\n", text.indexOf("W"));
        System.printf("   indexOf(\"o\"): %d\n", text.indexOf("o"));
        System.printf("   lastIndexOf(\"o\"): %d\n\n", text.lastIndexOf("o"));

        // 5. 包含判断
        System.printf("5. 包含判断:\n");
        System.printf("   contains(\"World\"): %d\n", text.contains("World"));
        System.printf("   contains(\"Earth\"): %d\n\n", text.contains("Earth"));

        // 6. 前缀和后缀
        System.printf("6. 前缀和后缀:\n");
        System.printf("   startsWith(\"Hello\"): %d\n", text.startsWith("Hello"));
        System.printf("   startsWith(\"Hi\"): %d\n", text.startsWith("Hi"));
        System.printf("   endsWith(\"!\"): %d\n", text.endsWith("!"));
        System.printf("   endsWith(\"?\"): %d\n\n", text.endsWith("?"));

        // 7. 大小写转换
        System.printf("7. 大小写转换:\n");
        String lower = text.toLowerCase();
        String upper = text.toUpperCase();
        System.printf("   小写: %s\n", lower);
        System.printf("   大写: %s\n\n", upper);

        // 8. 去除空白
        System.printf("8. 去除空白:\n");
        String spaced = "   Hello   ";
        String trimmed = spaced.trim();
        System.printf("   原始: \"%s\"\n", spaced);
        System.printf("   去空白: \"%s\"\n\n", trimmed);

        // 9. 替换
        System.printf("9. 替换:\n");
        String replaced = text.replace("World", "Lemon");
        System.printf("   原始: %s\n", text);
        System.printf("   替换后: %s\n\n", replaced);

        // 10. 比较
        System.printf("10. 字符串比较:\n");
        String s1 = "Apple";
        String s2 = "Banana";
        System.printf("   \"%s\" vs \"%s\": %d\n", s1, s2, s1.compareTo(s2));
        System.printf("   \"%s\" equals \"%s\": %d\n\n", s1, s1, s1.equals(s1));

        // 11. 整数转字符串
        System.printf("11. 整数转字符串:\n");
        int num = 12345;
        String numStr = String.intToString(num);
        System.printf("   String.intToString(%d) = \"%s\"\n\n", num, numStr);

        // 12. 字符串转整数
        System.printf("12. 字符串转整数:\n");
        String parseStr = "9876";
        int parsed = parseStr.toInt();
        System.printf("   \"%s\".toInt() = %d\n\n", parseStr, parsed);

        System.printf("=== 示例结束 ===\n");
    }
}